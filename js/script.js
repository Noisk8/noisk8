function porro(){
    document.getElementById('presente').innerHTML = 'Gracias por visitar esta web';
}

// Asegúrate de que la función se ejecute cuando el DOM esté completamente cargado
document.addEventListener('DOMContentLoaded', function() {
    // Aquí puedes llamar a la función porro() si es necesario
    // porro();
});